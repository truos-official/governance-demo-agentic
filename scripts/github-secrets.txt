GitHub Actions secrets required for CI/CD (Settings → Secrets and variables → Actions):

Secret name               Where to get the value
------------------------  -------------------------------------------------------
AZURE_CREDENTIALS         az ad sp create-for-rbac --name "github-actions-gov" \
                            --role Contributor \
                            --scopes /subscriptions/a022ff24-58dd-48b0-a8b5-0791dbdb06e6/resourceGroups/rg-governance-demo \
                            --sdk-auth
                          Paste the full JSON output as the secret value.

REACT_APP_API_URL         https://governance-backend.greenbay-a269b9fb.eastus.azurecontainerapps.io

OPENAI_API_KEY            Your OpenAI API key (sk-proj-...)
REDIS_HOST                governance-redis.redis.cache.windows.net
REDIS_PORT                6380
REDIS_PASSWORD            Azure portal → governance-redis → Access keys → Primary key
REDIS_SSL                 true
ELASTIC_ENDPOINT          https://<id>.es.<region>.azure.elastic.cloud:443
ELASTIC_API_KEY           Elastic Cloud console → Security → API keys
LANGCHAIN_API_KEY         LangSmith → Settings → API keys
AZURE_LANGUAGE_ENDPOINT   Azure portal → Azure AI Language → Keys and Endpoint
AZURE_LANGUAGE_KEY        Azure portal → Azure AI Language → Keys and Endpoint

Note: The AZURE_CREDENTIALS service principal needs these additional role assignments
to deploy Container Apps and push images:
  az role assignment create --role "AcrPush" --assignee <sp-app-id> \
    --scope /subscriptions/.../resourceGroups/rg-governance-demo/providers/Microsoft.ContainerRegistry/registries/governancedemoacr
